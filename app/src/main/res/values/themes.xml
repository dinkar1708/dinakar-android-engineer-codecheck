<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application window theme for Compose cold start. -->
    <style name="Theme.AndroidEngineerCodeCheck" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Status bar and navigation bar (Platform white with dark icons). All UI colors are strictly defined in Color.kt. -->
        <item name="android:statusBarColor" tools:targetApi="l">@android:color/white</item>
        <item name="android:windowLightStatusBar" tools:targetApi="m">true</item>
        <item name="android:navigationBarColor" tools:targetApi="l">@android:color/white</item>
        <item name="android:windowLightNavigationBar" tools:targetApi="o_mr1">true</item>
    </style>
</resources>